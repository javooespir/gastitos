{
  "name": "gastitos",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "cd backend && npm install && npx prisma generate && cd ../frontend && npm install && npm run build"
  }
}
